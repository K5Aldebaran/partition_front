# Tauri guest build/run mode
VITE_APP_MODE=guest

# IMPORTANT for demo: set backend via LAN IP (not localhost), e.g. http://192.168.0.10:8082
# Default keeps local run working; change before demonstration.
VITE_API_BASE=

VITE_USE_MOCK=false
